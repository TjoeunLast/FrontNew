import CommonSettlementScreen from "@/features/common/settlement/ui/CommonSettlementScreen";

export default function Page() {
  // 공통 정산 라우트: 역할별 정산 화면에서 공통 컴포넌트를 공유할 때 사용.
  return <CommonSettlementScreen />;
}
